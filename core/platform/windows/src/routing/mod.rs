//! The routing compensating-action transaction (`RoutingTransaction`) is
//! neutral logic over the `WindowsApiPort` trait, so it lives in
//! `nrr_platform_api::routing`. Shim for source compatibility.

pub use nrr_platform_api::routing::*;
